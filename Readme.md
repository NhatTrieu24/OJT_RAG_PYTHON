# 🚀 Gemini API Wrapper (FastAPI + Vertex AI)

A lightweight FastAPI service that connects to **Google Gemini** models in **Vertex AI** using the OpenAI-compatible API endpoint.

---

## 🧠 Features
- Uses **Gemini 2.0 Flash** for fast responses.
- Google Cloud authentication (Application Default Credentials).
- Simple `/generate` POST endpoint for text input.

---

## 📦 Setup

### 1️⃣ Install dependencies
```bash
pip install -r requirements.txt
